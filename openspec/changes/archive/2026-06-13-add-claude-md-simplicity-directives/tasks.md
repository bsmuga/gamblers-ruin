## 1. Update CLAUDE.md

- [x] 1.1 Add a simplicity bullet to the `## Conventions` section of `.claude/CLAUDE.md`:
      prefer the simplest working solution; avoid speculative abstraction and premature
      generalization (YAGNI).
- [x] 1.2 Add an anti-AI-commentary bullet to the same section: no inline comments that
      restate what the code does, narrate the change, or address an AI/agent reader;
      comments explain *why* (non-obvious rationale), not *what*.

## 2. Verify

- [x] 2.1 Re-read the edited `## Conventions` section to confirm both directives are
      present, concrete, and consistent in tone with the existing bullets.
